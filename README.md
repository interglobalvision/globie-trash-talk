# CUT THAT LOUD MESS OUT

### and send it over to a tumblr feed. Remember companies make APIs so more content can be created cus data = engagement [or maybe engagement = data]. Anyhow data===$$$.

#Run this shit like:

```
npm install
cp .env.example .env
node main.js
```

Add ur variables to `.env`
